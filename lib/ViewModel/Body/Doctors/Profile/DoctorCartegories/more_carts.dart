import 'package:flutter/material.dart';

class MoreCartsView extends StatelessWidget {
  const MoreCartsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
